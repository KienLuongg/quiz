# Docker Environment Configuration
spring.application.name=QuizExam_Student
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://mysql:3306/quiz
spring.datasource.username=root
spring.datasource.password=lht@39412990
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
jwt.expiration=604800000
jwt.secret="secret"
uploads.subject = /app/uploads/img-subject
uploads.question = /app/uploads/img-question
spring.web.resources.static-locations=classpath:/static/, file:/app/uploads/
spring.mvc.static-path-pattern=/uploads/**

# Actuator for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.db.enabled=true

# Logging
logging.level.com.example.quizexam_student=INFO
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
